# Tell Cucumber where to find step definitions
cucumber.glue=com.codecool.testing_good_behavior_test.steps

# Where feature files live
cucumber.features=src/test/resources/features

# Reporting options
cucumber.plugin=pretty, html:target/cucumber-report.html, json:target/cucumber-report.json

# Optional: fail if steps are undefined
cucumber.strict=true